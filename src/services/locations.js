export function AddLocations(location) {
    let locations = [
        {
            "name": location.name,
            "address": location.address
        }
    ];
    return locations;
}

export function GetLocations() {
    
}